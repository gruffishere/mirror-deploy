# ⛔ NEVER. The keys reach the server as environment variables the host holds.
.keys.json
.env

# the evidence lists live on the mounted disk in production and on this machine in development;
# committing them would push local test rows over the real ones on the next deploy
site/lists/

# rebuilt by npm ci
node_modules/

# rendered output and working files
site/png/
*.bak
*.log
*.tmp
